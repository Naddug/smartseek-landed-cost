# Railway deployment config
[deploy]
# preDeployCommand removed — db:push fails with Railway Postgres SSL and blocks on prompts.
# Reports table is created at runtime via ensureReportsTable() in server/index.ts.
